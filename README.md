# DICOM_Visualizer

These functions must be in the current folder during the execution. 
Please call Visualize() function on the terminal and the program will start. Note that it doesn't need object.
ReadImages() is the only function taken from a library and it needs the other presented functions.

Please note that DICOM file must be contained in a unique folder, in which series must complete.
